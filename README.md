# DoorScope Database Library

This library implements the database used by DoorScope. It builds on the backend of Sqlite 3.5.7 (you can download it from here: http://doorscope.ch/download/Sqlite3.tar.gz). 


